{
  "scripts": {
    "clean": "rimraf dist",
    "start": "eleventy --serve",
    "build": "eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-fetch": "^4.0.1",
    "rimraf": "^5.0.7"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}